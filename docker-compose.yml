version: '3.8'
services:
        mon-service:
                build:
                        context: .
                        dockerfile: Dockerfile
                image: travel
                ports:
                        - "8006:8006"
                environment:
                        ROCKET_ADDRESS: "0.0.0.0"
                        ROCKET_PORT: "8006"